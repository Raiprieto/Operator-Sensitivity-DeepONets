{
  "benchmark": "ns2d_exp_small",
  "description": "EXPERIMENTO (NS2D, sobreajuste): 5x100 en branch y trunk (K=100), entrada completa 64x64.",
  "data_from": "ns2d",
  "model": {
    "branch": [
      4098,
      100,
      100,
      100,
      100,
      100
    ],
    "trunk": [
      2,
      100,
      100,
      100,
      100,
      100
    ],
    "activation": "swish",
    "sigma": {
      "jacobian": 0.05,
      "vanilla": 0.025
    }
  },
  "train": {
    "optimizer": "adam",
    "iterations": 200000,
    "display_every": 1000,
    "val_every": 5000,
    "val_infer_batch": 50,
    "unc_ref_subset": 1024,
    "selection_metric": "interval_score",
    "patience": 8,
    "min_select_step": 0,
    "batch_size": 128,
    "learning_rate": 0.001,
    "decay_rate": 0.7,
    "decay_steps": 50000
  },
  "sweep": {
    "models": [
      "jacobian"
    ],
    "lambdas": [
      4
    ],
    "seeds": [
      0
    ],
    "note": "Experimento de capacidad/entrada contra ns2d (misma data, mismo lambda). Una semilla."
  },
  "source": "Derivada de protocol/configs/ns2d.json; solo cambia arquitectura, entrada y patience."
}
